class MembersController < ApplicationController
  def q3
    # @courses = Course.all
    # @students = Student.all
    # @enrolls = Enroll.all
    # # @a = Student.where(Student.course_id =>Course.course_id)
    # # @courses.enrolls = a.all
    # extractedId = params[:arr]
    # extractedId2 = params[:arr2]
    # @enrollforcourse = Enroll.where(extractedId2=>extractedId)

end
end